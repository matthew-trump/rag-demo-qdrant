# Optional: enable real OpenAI calls (if unset, mock mode is used)
# OPENAI_API_KEY=...
OPENAI_MODEL=gpt-5
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

CHUNK_SIZE=800
CHUNK_OVERLAP=120

# Qdrant (required for real vector storage)
# QDRANT_API_KEY=...        # optional for local; required for cloud
QDRANT_URL=http://localhost:6333
QDRANT_COLLECTION=rag-demo
